# YOLO-Bluebierd — Python deps
# CPU без AVX (AMD Phenom II X4 955) -> закріплено старіший стек, сумісний з numpy 1.26
# Для GPU/cloud (HF Jobs, Colab) — переходимо на numpy 2.x без обмежень

# Core CV
ultralytics==8.4.75
torch==2.12.1+cpu          # --index-url https://download.pytorch.org/whl/cpu
torchvision==0.27.1+cpu
opencv-python<4.11          # numpy 1.x compat
pillow>=10,<13

# Numerics — pinned для Phenom II
numpy<2
scipy<1.14

# Tooling
supervision<0.25
albumentations==2.0.8
roboflow==1.3.10

# Configs / utils
pyyaml>=6
tqdm
python-dotenv

# YOLO MCP server (in .tools/YOLO-MCP-Server)
mcp>=1.0

# Hugging Face — приватний хаб + Jobs
# УВАГА: `datasets` і `pyarrow` НЕ ставимо локально (SIGILL без SSE4.2). На cloud — без обмежень.
huggingface_hub>=1.20

# Gemini Imagen API — DEPRECATED (diffusion не дає bbox labels). Тримаємо для bg inpaint якщо знадобиться.
google-genai>=2.10

# === Blender composite pipeline (НЕ ставити локально — Phenom II без AVX) ===
# Сlloud-only через Colab Free T4 / HF Jobs L4. Список тут для documentation.
#   blenderproc>=2.7    — DLR-RM wrapper навколо Blender 4.2 LTS, YOLO/COCO export
#   bpycv>=0.2          — instance segmentation + depth
#   scipy>=1.10         — KS-test у tests/test_distribution_match.py (на cloud або новій машині)
